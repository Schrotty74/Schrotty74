# Template for a new Codex chat

Work only in the `Schrotty74-profile` repository.

Before starting, read `PROJECT_CONTEXT.md` and `NEXT_STEPS.md`. Read the additional files named there when they apply to the task.

The project context contains the public-profile, verification, privacy, and publication rules.
